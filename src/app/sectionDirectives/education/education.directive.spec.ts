import { EducationDirective } from './education.directive';

describe('EducationDirective', () => {
  it('should create an instance', () => {
    const directive = new EducationDirective();
    expect(directive).toBeTruthy();
  });
});
