import { SummaryDirective } from './summary.directive';

describe('SummaryDirective', () => {
  it('should create an instance', () => {
    const directive = new SummaryDirective();
    expect(directive).toBeTruthy();
  });
});
