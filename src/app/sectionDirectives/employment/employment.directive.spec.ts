import { EmploymentDirective } from './employment.directive';

describe('EmploymentDirective', () => {
  it('should create an instance', () => {
    const directive = new EmploymentDirective();
    expect(directive).toBeTruthy();
  });
});
