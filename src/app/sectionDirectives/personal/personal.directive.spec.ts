import { PersonalDirective } from './personal.directive';

describe('PersonalDirective', () => {
  it('should create an instance', () => {
    const directive = new PersonalDirective();
    expect(directive).toBeTruthy();
  });
});
