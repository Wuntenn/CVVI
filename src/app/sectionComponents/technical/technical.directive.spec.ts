import { TechnicalDirective } from './technical.directive';

describe('TechnicalDirective', () => {
  it('should create an instance', () => {
    const directive = new TechnicalDirective();
    expect(directive).toBeTruthy();
  });
});
